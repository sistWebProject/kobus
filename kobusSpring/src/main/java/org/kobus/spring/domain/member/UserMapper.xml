<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kobus.spring.domain.member.UserMapper">
    <select id="getKusIDById" parameterType="string" resultType="string">
        SELECT kusID FROM kobusUser WHERE id = #{id}
    </select>
</mapper>
